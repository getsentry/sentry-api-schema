# dependencies (bun install)
node_modules

# output (regenerated by `bun run build`)
dist
src

# normalized spec written by build step 0.5 (derived from openapi-derefed.json)
openapi-normalized.json

# caches
.eslintcache
.cache

# Finder (MacOS) folder config
.DS_Store
